import React from 'react';
import './Subscription.css';

const Subscription = () => {
    return (
        <div>
            <h2>this is subscription area</h2>
        </div>
    );
};

export default Subscription;